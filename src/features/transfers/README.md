# Transfers

React components related to transferring money between accounts.

Possible components:

- `TransferForm.jsx`
- `TransferConfirmation.jsx`

This feature handles the frontend interface for the transfer endpoints.

test
